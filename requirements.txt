# Core runtime dependencies (CPU-only, runs fully offline).
# NOTE: install PyTorch separately from the CPU index for a smaller footprint:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4

# Serving
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
requests>=2.31
python-dotenv>=1.0

# Testing
pytest>=8.0
