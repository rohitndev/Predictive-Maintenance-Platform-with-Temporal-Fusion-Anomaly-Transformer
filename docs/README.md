# docs/

This folder holds the diagrams and screenshots referenced by the root
[`README.md`](../README.md).hig

The analytical charts (`tft-rul-prediction.png`, `anomaly-detection.png`) can
alternatively be produced directly from the project:

```bash
pip install matplotlib
python -m notebooks.tft_analysis
```
